// Central place for theme constants (CSS variables are the source of truth).
export const THEME = {
  green: "var(--status-running)",
  bgPrimary: "var(--bg-primary)",
  textPrimary: "var(--text-primary)",
  textAccent: "var(--text-accent)",
} as const;

